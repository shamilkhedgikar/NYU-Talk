# Data Layout

- `raw/`: uploaded layers, tabular covariates, original reference files
- `interim/`: intersections, joins, derived sparse structures
- `processed/`: analysis-ready operators, exports, benchmark outputs

Keep large or sensitive data out of the rendered book unless it is intentionally published.

